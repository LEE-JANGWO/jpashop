spring:
# 스프링부트에서는 별도의 설정이 없어도 메모리모드로 테스트 돌린다. (이 페이지에 아무것도 없어도 돌아간다)
#  datasource:
#    url: jdbc:h2:mem:test
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create-drop
#    properties:
#      hibernate:
##        show_sql: true //
#        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace